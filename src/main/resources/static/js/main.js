class Main{
    constructor(elTarget, htOption) {
        this.elTarget = elTarget;
        this.init(htOption);
    }

    init(htOption) {
        console.log('test');
        console.log("ttestdf");
        this.sendDebugger();
    }

    sendDebugger(){
        debugger;
    }
}